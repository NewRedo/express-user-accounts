include form-fields-mixin.pug

mixin form(formEnctype, formMethod, formNoValidate)
		form(action=action,enctype=(formEnctype || "multipart/form-data"),method=(formMethod || "post"),novalidate=formNoValidate)
				block
